# Ad free YouTube video player

Download/clone:

`git clone git@github.com:kayartaya-vinod/adfree-video.git`


Install dependencies:

`npm install`


Run the app:

`npm start`


Build after making changes:

`npm run build`

